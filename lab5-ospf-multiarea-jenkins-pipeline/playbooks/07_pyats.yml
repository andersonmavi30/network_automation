---
- name: Lab 5 - pyATS structured validation
  hosts: localhost
  gather_facts: false

  vars_files:
    - ../group_vars/all.yml
    - ../group_vars/vault.yml

  roles:
    - role: ../roles/lab5_pyats
