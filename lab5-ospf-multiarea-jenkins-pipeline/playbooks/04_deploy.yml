---
- name: Lab 5 - Deploy OSPF multi-area
  hosts: "{{ target_hosts | default('all') }}"
  gather_facts: false

  vars_files:
    - ../group_vars/all.yml
    - ../group_vars/vault.yml

  roles:
    - role: ../roles/lab5_ospf