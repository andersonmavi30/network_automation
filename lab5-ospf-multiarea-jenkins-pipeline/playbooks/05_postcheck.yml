---
- name: Lab 5 - OSPF postcheck with Ansible
  hosts: "{{ target_hosts | default('all') }}"
  gather_facts: false

  vars_files:
    - ../group_vars/all.yml
    - ../group_vars/vault.yml

  roles:
    - role: ../roles/lab5_postcheck