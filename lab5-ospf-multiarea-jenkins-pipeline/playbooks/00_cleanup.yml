---
- name: Lab 5 - Cleanup / Rollback
  hosts: "{{ target_hosts | default('all') }}"
  gather_facts: false

  vars_files:
    - ../group_vars/all.yml
    - ../group_vars/vault.yml
    - ../vars/lab5_topology.yml

  roles:
    - role: ../roles/lab5_cleanup